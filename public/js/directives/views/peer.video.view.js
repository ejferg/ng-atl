atl.directive('atlPeerVideoView', ['$log', 'rtc', function($log, rtc) {
        
        return {
            
            link: function(scope, elem, attrs) {
                
            }
        }
}]);